# 配置总览(自动生成 · config_schema.py 注册表)

分层来源(优先级 高→低): **运行时覆盖 > 环境变量 > settings.json > 内置默认**

## collect
| 配置项 | 类型 | 默认 | 环境变量 | settings 路径 | 说明 |
|---|---|---|---|---|---|
| `collect.fresh` | bool | `False` | `COLLECT_FRESH` | `` | 全量重抽设定(删旧 settings_graph 增量) |

## embed
| 配置项 | 类型 | 默认 | 环境变量 | settings 路径 | 说明 |
|---|---|---|---|---|---|
| `embed.off` | bool | `False` | `EMBED_OFF` | `` | 跳过本地 bge-m3 向量化(省本地 CPU; FTS 检索不受影响) |

## extract
| 配置项 | 类型 | 默认 | 环境变量 | settings 路径 | 说明 |
|---|---|---|---|---|---|
| `extract.mode` | str | `v2` | `` | `` | 场景抽取模式 |
| `extract.seg_mode` | str | `rule` | `` | `` | 场景切分模式 |

## knowledge
| 配置项 | 类型 | 默认 | 环境变量 | settings 路径 | 说明 |
|---|---|---|---|---|---|
| `knowledge.scope` | json | `{'enabled': False, 'domains': []}` | `` | `` | 公版体系域作用域(默认不开放; --genre 动态加载) |

## llm
| 配置项 | 类型 | 默认 | 环境变量 | settings 路径 | 说明 |
|---|---|---|---|---|---|
| `llm.backend` | str | `local` | `` | `llm/backend` | 主 LLM 预设名(models.json 枚举) |
| `llm.api_key` | str | `` | `LLM_API_KEY` | `llm/api_key` | 云端 API key(敏感, 不在命令行明文回显) |
| `llm.base_url` | str | `http://localhost:11434` | `LLM_BASE_URL` | `llm/base_url` | LLM 服务地址 |
| `llm.model` | str | `` | `LLM_MODEL` | `llm/model` | 覆盖模型名 |
| `llm.auth_scheme` | str | `` | `LLM_AUTH_SCHEME` | `` | 认证方式(bearer/apikey; 空=按 backend 推导) |
| `llm.enable_thinking` | bool | `False` | `LLM_ENABLE_THINKING` | `` | 开启深度思考(默认关, 省 token) |
| `llm.num_ctx` | int | `4096` | `LLM_NUM_CTX` | `` | 上下文窗口 |
| `llm.temperature` | float | `0.3` | `LLM_TEMPERATURE` | `` | 采样温度 |
| `llm.doubt_index` | float | `0.5` | `LLM_DOUBT_INDEX` | `` | 质疑指数 0-1(簇触发阈值/思考深度) |
| `llm.role_models` | json | `{}` | `LLM_ROLE_MODELS` | `` | 角色路由 {"review":"glm-4-flash"} |
| `llm.embed_model` | str | `bge-m3` | `` | `llm/embed_model` | 本地向量化模型(Ollama) |

## novel
| 配置项 | 类型 | 默认 | 环境变量 | settings 路径 | 说明 |
|---|---|---|---|---|---|
| `novel.name` | str | `小说` | `NOVEL_NAME` | `novel/name` | 书名(决定 db/产物命名) |
| `novel.path` | str | `` | `BOOK_PATH` | `novel/path` | 小说 txt 路径(相对项目根或绝对) |
| `novel.chapters` | int | `50` | `NOVEL_CHAPTERS` | `novel/chapters` | 处理章数(决定 db 与产物规模) |
| `novel.genre` | str | `` | `NOVEL_GENRE` | `` | 小说类别, 触发加载对应公版体系域 |

## rag
| 配置项 | 类型 | 默认 | 环境变量 | settings 路径 | 说明 |
|---|---|---|---|---|---|
| `rag.external_search` | bool | `False` | `EXTERNAL_SEARCH` | `` | 启用联网搜索钩子(knowledge_router L3) |
| `rag.top` | int | `10` | `` | `` | RAG 问答返回条数 |

## run
| 配置项 | 类型 | 默认 | 环境变量 | settings 路径 | 说明 |
|---|---|---|---|---|---|
| `run.src_dir` | str | `cloud_fixed` | `` | `run/src_dir` | 可视化任务读哪个 LLM 产物目录 |
| `run.date_suffix` | str | `` | `` | `run/date_suffix` | 输出目录固定后缀(留空=自动时间戳) |
| `run.out_dir` | str | `` | `` | `` | 产物输出目录覆盖 |
| `run.parallel` | int | `4` | `` | `` | 场景级/简历并发数 |
| `run.log` | str | `` | `` | `` | analyze 任务指定日志 jsonl 路径 |
| `run.archive.w_recent` | int | `30` | `` | `run/archive/w_recent` | 最近 N 章出现过→活跃 |
| `run.archive.k_freq` | int | `3` | `` | `run/archive/k_freq` | 在 ≥N 章出现→活跃 |
| `run.archive.k_confirm` | int | `2` | `` | `run/archive/k_confirm` | 被后文证实 N 次→永久活跃 |
| `run.archive.every` | int | `20` | `` | `run/archive/every` | 每 N 章做一次代际压缩 |
