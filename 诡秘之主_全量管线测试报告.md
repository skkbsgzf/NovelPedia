# 诡秘之主 · pedia 全量管线测试报告

- 生成日期：2026-08-31
- 测试对象：诡秘之主（1308 章）
- 测试范围：pedia 开源管线（故事提取 + RAG + 实体归一化）；深度推理已剥离至 studio

---

## 一、测试配置

| 项 | 值 | 来源 |
|---|---|---|
| 书名 | 诡秘之主 | settings.json · novel.name |
| 处理章数 | 1308 章 | settings.json · novel.chapters |
| 抽取场景块 | 10161 个（avg 7.77 块/章） | data/stage1_v2_1308_诡秘之主.db |
| 主模型 | dots3-note（默认） | models.json · routing.default |
| 架构边界 | pedia=故事提取+RAG+归一化；studio=人物深度"揉碎" | 本期已落地 |

---

## 二、测试时长（全量真实，来自 full_run_1308 系列日志）

| 阶段 | 耗时 | 说明 |
|---|---|---|
| extract · 场景抽取 | **167.7 min** | 10161 场景块 · v2 抽取（一次性，已落库） |
| collect · Stage1（registry + setting + character + style） | **169.6 s** | 含旧暗线；剥离后更快 |
| mine · Stage2（setting + character + style） | **22.6 s** | 三者全局聚合，轻量 |
| **合计** | **≈ 171 min** | extract 占绝对主体（98%） |

> collect / mine 时长为全量真实跑批记录；extract 为一次性抽取，已落库 `stage1_v2_1308_诡秘之主.db`（65MB），后续 collect/mine 重跑**无需再跑 extract**。

---

## 三、Token 消耗量

### 3.1 现状与能力

- 全量跑批当时**未启用持久 token 计量**（原 `TOKEN_METER` 仅 panel 注入，全量子进程未注入），故无原始全量 token 记录。
- **本次已上线持久计量器**：`llm_client` 每次 `chat()` 自动累加并原子落盘 `logs/token_total.json`（线程安全、崩溃安全、支持 `reset_token_meter()`）。
- 既有 demo 小样本**真实** token（by `glm-4-flash`，`logs/run_20260831_*/progress.json`）：

| demo 运行 | prompt | completion | total |
|---|---|---|---|
| run1 | 3,522 | 2,237 | 5,759 |
| run2 | 13,443 | 6,506 | 19,949 |
| run3 | 15,606 | 5,530 | 21,136 |

### 3.2 全量 token 量级估算（基于真实调用规模，非实测）

collect 阶段 LLM 调用（确定项）：

- `entity_registry`：**每章 1 次 = 1308 次**（主 token 源，每章输入≈该章全部场景原文）
- `setting` 强化：1 次（全局）
- `style` 分析：1 次（全局）
- `character` 聚合：纯规则，**无 LLM 调用**

mine 阶段（随人物数）：

- 设定强化 1 次 + 人物简历（按人物批量，诡秘之主人物量级数百）× N + 文风分析 1 次

按 demo 单次调用 token 中值（prompt≈10.5K / completion≈4.4K，glm 口径）外推：

- registry 1308 × 中值 ≈ prompt 13.7M / completion 5.8M
- **仅 registry 即 ≈ 19.5M token**（glm/dots 口径相近）
- 叠加 setting / style / mine（人物简历为主）≈ **全量 collect+mine 量级约 2–3 千万 token**

> ⚠️ 此为估算，精确值请执行下方采集命令（复用已抽取库，无需重跑 167min extract）。dots3-note 与 glm tokenizer 略有差异，量级在同一数量级。

### 3.3 精确采集命令（计量器已就绪）

```bash
# 1) 重置计量（清空 logs/token_total.json，使其仅含本次运行）
python -c "import sys; sys.path.insert(0,'src'); import llm_client; llm_client.reset_token_meter()"

# 2) 配置密钥后，全量重跑 collect + mine（复用 10161 场景库，不重跑 extract）
python src/stage1_collect.py --backend dots3-note
python src/stage2_mine.py  --backend dots3-note

# 3) 读取累计 token（含 by_model 明细）
cat logs/token_total.json
```

预计：collect/mine 全量约数分钟~数十分钟（dots 1 并发下 registry 1308 次为主耗时）；extract 已落库，跳过。

---

## 四、本期交付的架构变更（pedia 侧）

1. **深度推理剥离**：删除 `clue_agent.py` / `scene_annotator.py` / `main_agent.py` / `stage1_feel.py`；暗线收集 / 剧情推理 / 拉片标注迁至 studio。pedia 仅保留故事提取 + RAG + 归一化。
2. **实体归一化保全**：独立 `entity_registry.py`（"克莱恩=周明瑞"核心能力保留，Pass0 每章抽实体+别名），与 clue_graph 解耦。
3. **可插拔 + 重跑覆盖**（stage1 & stage2 对称）：
   - `--only <module>` 单模块重跑（stage1: registry/setting/character/style；stage2: setting/character/style）
   - `--force` 强制覆盖
   - skip-if-exists 幂等跳过（非 force 且产物存在且合法则跳过）
   - `_atomic_write` 防半截文件
   - `module_manifest.json` 记录每个模块 ok / skip / fail
4. **持久 token 计量器**：`llm_client` 每次调用落盘 `logs/token_total.json`，支持 `reset_token_meter()`。

---

## 五、验证

| 项 | 结果 |
|---|---|
| 编译 | 全部模块 `py_compile` 通过 |
| 导入冒烟 | cli / stage1_collect / stage2_mine / entity_registry / llm_client 导入成功 |
| 计量器落盘链路 | mock 3 次调用正确累加并落盘 by_model 分组；`reset_token_meter()` 清空有效 |
| 主链路解耦 | build_detail / export_graph / vizutil 不读 clue；editor_review / style_sampler 对 clue 容错（空字典不崩） |

---

## 附：可插拔重跑示例

```bash
# 仅重跑设定强化（已存在则跳过）
python src/stage2_mine.py --only setting
# 强制重跑人物简历并覆盖
python src/stage2_mine.py --only character --force
# 仅重跑实体注册表
python src/stage1_collect.py --only registry --force
```
