# 📋 开发进展（StoryScienceLab / 小说拆书工程）

> 阶段性目标 → 当前状态 → 下一里程碑。
> 最后更新：2026-08-30

---

## 现状（一句话）

**StoryScienceLab 小说拆书工程已开源**（`D:\StoryScienceLab\novel_pipeline`），
包含 16 个主流模型预设 / 4 种后端协议（ollama/openai/anthropic/google）、
角色路由、唯一 demo（《诡秘之主》300 章自包含工作台），并已部署到 CloudStudio / GitHub。

---

## ✅ 已完成（开源版 v1.0）

### 核心引擎
- [x] **多类型模型匹配**：`models.json` 注册表，16 预设（OpenAI GPT-5/Anthropic Claude Opus 4.8/Google Gemini 3 Pro/DeepSeek V3.2/GLM-5.2/Kimi K2.5/Qwen3-Max/Groq/本地 qwen3:8b…），4 后端协议
- [x] **角色路由**：`--role role=preset` 可多次指定（extract/collect/mine/review/summary/embed），llm_client 动态切换后端
- [x] **统一 CLI** `src/cli.py`：14 任务（extract/feel/collect/mine/annotate/review/build/personality/graph/detail/report/rag/viz/analyze），`--list-backends` 枚举预设
- [x] **运行时清晰提示**：`▶ 后端 [xxx] 名称 🆓/💰 model/上下文/并发/JSON` + 子任务耗时 `⏱` + HTTP 错误中文定位（401/403/404/429）
- [x] **密钥清零**：3 处硬编码 API Key → 环境变量；git 历史零泄漏
- [x] **路径相对化**：`resource/小说.txt`（小说原文 gitignore 排除）

### 拆书能力
- [x] **场景抽取**（`extract`）：rule + 向量突变兜底，actinfo v2
- [x] **Stage1 四维收集**（`collect`）：设定 / 暗线 / 人物 / 文风采样 并行
- [x] **Stage2 四维挖掘**（`mine`）：剧情推理 / 设定强化 / 人物简历 / 文风分析，并行
- [x] **创作解析标注**（`annotate`）：关键场景 plot_function + note，拉片画布 🎬
- [x] **编辑评述 agent**（`review`）：5 板块专业编辑视角 habits/strengths/weaknesses/summary
- [x] **知识图谱**（`graph`）：角色关系 + 设定体系，章节筛选
- [x] **拆书详情页**（`detail`）：四 Tab 全景（剧情/人物/设定/文风）
- [x] **拉片画布**（P0+P1+P2）：分镜卡 / 伏笔-回收链 / 创作解析，缩放平移 + 弹层
- [x] **公版体系图 RAG**（`--genre`）：18 域/551 词/644 边，触发式加载防幻觉
- [x] **跨书基准**（`style_baseline`）：genre + all 桶，飞轮式积累
- [x] **跑批日志**（`logbook` + `cli analyze`）：双通道 .log/.jsonl，H1-H5 健康度

### 可视化
- [x] **单文件自包含工作台**：`outputs/<书>_<日期>/index.html` 7MB+，零外部依赖，双击即开
- [x] **移动端汉堡抽屉**（`.side-toggle` + `.side-mask`）
- [x] **唯一 demo**：诡秘之主 300 章（865 人物 / 81 设定 / 27 推理 / 6134 暗线），含编辑评述卡

### 工程
- [x] **干净仓库**：61→62 个跟踪文件（demo 大文件除外），运行产物/data/logs/死代码全部 gitignore
- [x] **MIT LICENSE**
- [x] **完整文档**：README + `docs/技术文档.md` / `docs/使用说明.md` / `docs/参数配置与运行效率.md` / `docs/日志与跑批分析.md` / `docs/doubt_index_design.md`
- [x] **在线试用 HTTP 服务**：`_web_demo.py` + `_demo_run.py`（商业化入口，无 key 时报错）
- [x] **已部署**：CloudStudio 公网链接 + GitHub (skkbsgzf/NovelPedia)

---

## 🔜 下一阶段（待办）

- [ ] **跨书基准飞轮**：再跑 1-2 本书验证 genre 桶的迁移性
- [ ] **GitHub Actions CI**：lint + 关键路径冒烟（lint.yml / smoke.yml）
- [ ] **Pypi 包发布**（可选）：让 `pip install storysciencelab` 即用
- [ ] **Web demo 公共化**：免费额度 + 排队机制（当前仅本地或受信任实例）
- [ ] **真正的 Anthropic extended thinking**（beta）：llm_client 当前关 thinking，对 thinking 模型简单粗暴禁掉；可补 beta header 启用
- [ ] **多语言界面**（英文/日文 demo 截图）：当前 README 中文为主，国际化要补
- [ ] **配置迁移收尾**：剩余硬编码常量（OLLAMA_NUM_PARALLEL/SEG_* 等）逐步入注册表；`--seg-mode` 等参数在子进程入口的统一透传
- [ ] **运行配置快照**（v1.2）：每次跑批把生效配置写 `logs/run_*.config.json`，可追溯复现

---

## 🗑️ 已移除（_legacy/ 保留本地可查，不入库）

- 24 个实验 / probe / 死代码脚本（probe_lora / loadtest / bench_stage2 / smoke_test / stage3_viz_report …）
- 8 个 `_tmp*.js` 临时补丁
- 7 篇设计过程稿 docs（PEDIA 回流 / 剧情拉片画布 / 文风 v3 / 面板优化点 / 诡秘之主 300 章缺陷清单 / 主 agent 设计 / four_dim_pipeline）
- 2 个一次性跑批脚本（`_run_gm_collect.py` / `_run_feel.py` 剥 key 后的参考模板）

## 📐 已知取舍

1. 本地小模型偶发吐坏 JSON；云端 8b 全成功
2. dots.ai 限流 RPM=60；300 章全流程 1.5~3h
3. 排比/修辞为规则近似检测，数值仅作风格参考
4. 人物重名依赖别名图确定性归并（30→23 人）
5. 真相核验：模型 ID 以厂商最新文档为准；本表 2026-08 查证后已更新

---

## 下一里程碑

**v1.1**：跨书基准验证 + GitHub Actions CI + 国际 demo 截图
**v1.2**：Pypi 发布 + 公共 Web demo 限额
