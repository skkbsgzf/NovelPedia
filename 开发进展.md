# 📋 开发进展（StoryScienceLab / 小说拆书工程）

> 阶段性目标 → 当前状态 → 下一里程碑。
> 最后更新：2026-09-01

---

## 现状（一句话）

**StoryScienceLab 小说拆书工程已开源 v1.1**（`D:\StoryScienceLab\pedia`，GitHub: skkbsgzf/NovelPedia）：
深度推理（暗线/剧情推理/拉片标注）已整体迁至闭源 **studio**，pedia 开源仓保留
**故事提取 + RAG + 实体归一化**（三维拆解 + 最简章纲），并补齐**可插拔模块重跑**与**持久 token 计量**。

---

## ✅ 已完成（开源版 v1.1 · 2026-09-01）

### 架构边界（pedia / studio 分工）
- [x] **深度推理剥离**：删除 `clue_agent.py` / `scene_annotator.py` / `main_agent.py` / `stage1_feel.py`，`cli.py` 移除 `feel`/`annotate` 任务；剧情推理/暗线/拉片标注迁 studio
- [x] **实体归一化保全**：从 clue_agent 剥离出 `entity_registry.py`（每章抽稳定实体+别名，克莱恩=周明瑞 可分得清），`entity_registry.json` 独立产出
- [x] **章纲保留**：最简章纲（`outlines.json`）保留在 stage2 链路，供 build_detail/export_graph 只读消费

### 可插拔 + 失败重跑覆盖（stage1 / stage2 全覆盖）
- [x] `--only <module>` 单模块重跑（collect: registry/setting/character/style；mine: setting/character/style）
- [x] `--force` 强制覆盖 + skip-if-exists 幂等跳过（产物存在且合法则跳过）
- [x] `_atomic_write`（先 .tmp 再 os.replace）防半截文件
- [x] `module_manifest.json` 记录每个模块 ok/skip/fail
- [x] `cli.py` 全局透传 `--only`/`--force`

### 工程可观测
- [x] **持久 token 计量**：`llm_client` 每次调用原子落盘 `logs/token_total.json`（by_model 分组，不依赖 panel 注入）
- [x] **setup_backend 修复**：改用 `get_preset()` 解析 models.json 预设（原来 `load_config()` 读不到 presets，`LLM_MODEL` 被默认成 qwen3:8b 劫持云端请求 → HTTP 400 白烧）
- [x] **1308 章全量测试报告**：`诡秘之主_全量管线测试报告.md`（extract 167.7min / collect 169.6s / mine 22.6s / 合计 ≈171min）
- [x] **10 章双模型对比**：`compare_10ch/compare_result.md`（dots3-note vs 本地 qwen3:8b 的实体抓取精度与效率）

### 核心引擎（v1.0 保留）
- [x] **多类型模型匹配**：`models.json` 注册表，16 预设（OpenAI GPT-5/Anthropic Claude Opus 4.8/Google Gemini 3 Pro/DeepSeek V3.2/GLM-5.2/Kimi K2.5/Qwen3-Max/Groq/本地 qwen3:8b…），4 后端协议
- [x] **角色路由**：`--role role=preset` 可多次指定（extract/collect/mine/review/summary/embed）
- [x] **统一 CLI** `src/cli.py`：extract/collect/mine/review/build/personality/graph/detail/report/rag/viz/analyze
- [x] **场景抽取**（`extract`）：rule + 向量突变兜底，actinfo v2，缓存可复用
- [x] **编辑评述 agent**（`review`）：5 板块专业编辑视角
- [x] **公版体系图 RAG**（`--genre`）：18 域/551 词/644 边，触发式加载防幻觉
- [x] **跨书基准**（`style_baseline`）：genre + all 桶，飞轮式积累
- [x] **跑批日志**（`logbook` + `cli analyze`）：双通道 .log/.jsonl，H1-H5 健康度

### 可视化
- [x] **单文件自包含工作台**：`outputs/<书>_<日期>/index.html` 7MB+，零外部依赖，双击即开
- [x] **移动端汉堡抽屉**（`.side-toggle` + `.side-mask`）
- [x] **唯一 demo**：诡秘之主 300 章自包含工作台（人物/设定/文风 + 编辑评述）

### 工程
- [x] **干净仓库**：运行产物/data/logs/死代码/密钥文件全部 gitignore（`.env`/`*.key`/`*api_key*` 红线）
- [x] **MIT LICENSE**（pedia 开源；商业化聚焦便利性/数据/服务，代码本体开源）
- [x] **完整文档**：README + docs/（技术文档/使用说明/参数配置/日志分析/doubt_index）均已加 v1.1 注记
- [x] **已部署**：CloudStudio 公网链接 + GitHub (skkbsgzf/NovelPedia)

---

## 🔜 下一阶段（待办）

- [ ] **studio 深度人格分析**：在 studio 中重新设计暗线/剧情推理/拉片标注（pedia 已让位）
- [ ] **跨书基准飞轮**：再跑 1-2 本书验证 genre 桶的迁移性
- [ ] **GitHub Actions CI**：lint + 关键路径冒烟（lint.yml / smoke.yml）
- [ ] **Pypi 包发布**（可选）：让 `pip install storysciencelab` 即用
- [ ] **Web demo 公共化**：免费额度 + 排队机制（当前仅本地或受信任实例）
- [ ] **多语言界面**（英文/日文 demo 截图）：当前 README 中文为主
- [ ] **配置迁移收尾**：剩余硬编码常量逐步入注册表
- [ ] **运行配置快照**（v1.2）：每次跑批把生效配置写 `logs/run_*.config.json`

---

## 🗑️ 已移除（_legacy/ 保留本地可查，不入库）

- 深度推理模块：`clue_agent.py` / `scene_annotator.py` / `main_agent.py` / `stage1_feel.py`（迁 studio）
- CLI 任务：`feel` / `annotate`
- 24 个实验 / probe / 死代码脚本（probe_lora / loadtest / bench_stage2 / smoke_test / stage3_viz_report …）
- 8 个 `_tmp*.js` 临时补丁
- 7 篇设计过程稿 docs（PEDIA 回流 / 剧情拉片画布 / 文风 v3 / 面板优化点 / 诡秘之主 300 章缺陷清单 / 主 agent 设计 / four_dim_pipeline）
- 2 个一次性跑批脚本（`_run_gm_collect.py` / `_run_feel.py` 剥 key 后的参考模板）

## 📐 已知取舍

1. 本地 qwen3:8b 实体抽取别名发现弱（10 章实测别名 60 vs dots 158），克莱恩=周明瑞 归一化仅 dots 正确
2. dots.ai 限流 RPM=60；1308 章全量 extract 约 2.8h（collect+mine 复用库仅 3 分钟）
3. 排比/修辞为规则近似检测，数值仅作风格参考
4. 人物重名依赖别名图确定性归并，个别同人可能仍拆分
5. 本地 qwen3:8b 走默认后端解析时会打印"降级到本地 ollama"告警（每章一次），结果正确但刷屏——后续可将显式 --backend 直接传入调用链消除

