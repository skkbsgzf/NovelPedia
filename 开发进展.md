# 📋 开发进展（StoryScienceLab / 小说拆书工程）

> 待办式开发清单：阶段性目标 → 当前任务 → 进展记录。
> 最后更新：2026-08-23

---

## 一、阶段性目标（终极形态）

### 🎯 阶段 D —— 门户网站（终极形态，未开始）
- [ ] **网址前端**：用户可查看方法论 / 知识体系 / demo 示例
- [ ] **上传框**：接受 epub + txt
- [ ] **后端转换**：接收上传 → 路由到转换程序 → 进度实时回传
- [ ] **产物收敛**：最终只输出**一个 index.html**（自包含、零外部依赖）
- [ ] **部署形态**：免费 API（非本地小模型），不涉及本地模型验证
- [ ] **清理硬编码**：去掉「本地 vs 云端」对比报告、去掉硬编码《诡秘之主》文案

### 🎯 阶段 C —— Web Demo（当前阶段）
- [x] 设计需求清单（门户愿景 + 上传 demo 范围）
- [ ] `_web_demo.py` —— HTTP 服务（接收上传 / 轮询进度 / 托管产物）
- [ ] `_demo_run.py` —— 后台跑流水线，写 progress.json / result.json
- [ ] `web_demo.html` —— 拖拽上传 + 进度条页面
- [ ] **联调**：上传 → 解析 → 进度展示 → 打开产物 index.html 全链路

### 🎯 阶段 B —— 双格式原生支持（已基本完成）
- [x] `ingest.py` 原生解析 epub（zipfile + OPF spine + xhtml 提取），按扩展名自动分流
- [x] 修复 epub max_chapters 边界（多读第 51 章问题）
- [x] 修复跨小说污染：db / 抽取缓存按小说名隔离
- [x] 修复 stage2 上下文参数（qwen3:8b 下 num_ctx=16384 → 跟随 config 4096）
- [x] 修复 stage2 硬编码《诡秘之主》提示词 → 读 C.NOVEL_NAME
- [x] 实测《十日终焉》(epub) 50 章全链路跑通，16/16 健康检查

### 🎯 阶段 A —— 源码修复（已完成）
- [x] 9 个损坏源码文件修复（cli / build_detail / export_graph / final_report / compare_stage2 / check_viz / gen_personality 等）
- [x] 11 个核心环节编译 + 导入全绿
- [x] `cli.py viz` 一键链路端到端跑通
- [x] `check_viz.py` 健康检查 16/16
- [x] 目录清理（400 个垃圾文件 / `_` 调试脚本 / cdp 浏览器数据 / venv 保留）

---

## 二、Todo（当前待办，按优先级）

### P0 · 四维管线（新架构，已完成）
- [x] **四维度 stage1收集/stage2挖掘**：`stage1_collect.py`（设定/暗线/人物/文风四路并行）+ `stage2_mine.py`（推理/设定强化/简历/文风分析四路并行）
- [x] **暗线收集**（`clue_agent.collect_evidence`）：只捕 actinfo 表达不了的信号（反常/矛盾/信息差/伏笔/隐藏关系），0推理 + token 硬上限 + 与 actinfo 向量比对双保险过滤
- [x] **一次性聚类**：修增量聚类"中心漂移"（442 条全进一簇）→ 收集完后两两相似度贪心聚类（阈值0.62），446 条 → 119 簇，主题清晰分离
- [x] **设定图谱并发**：`setting_agent.run_setting_agent(parallel)` 并发抽取→串行卷积，20 章 3036s → 632s（4.8x）
- [x] **性能目标达成**：20 章 stage1 从 50 分钟 → **13.4 分钟**（目标 40 分钟）
- [x] **文风独立采样**（`style_sampler`）：全文随机 10%，长叙述:短句=7:3，与图谱流程分离
- [x] **人物简历**（`character_agent`）：Stage1 纯规则聚合 actinfo，Stage2 LLM 生成简历（身份/轨迹/关系/疑点）
- [x] **可视化四 Tab**：剧情推理（结论→证据簇→原文推理链+线索图谱）/ 设定体系（分层+矛盾+图谱）/ 人物简历 / 文风分析

### P0.5 · 调研优化落地（ERA-CoT × HyperGraphRAG）
- [x] **优化 A 全局实体锚定**：`clue_agent.extract_entities_per_chapter` 每章抽稳定实体+别名 → entity_registry.json，注入暗线收集 prompt（实体名统一，省 normalize 兜底）
- [x] **优化 B confidence 置信过滤**：暗线每条带 confidence(0-1)，簇按均置信过滤（ERA-CoT 第4步），doubt_index 联动阈值（0.3→0.75, 0.5→0.65, 0.7→0.55, 0.9→0.45），旧数据向后兼容
- [x] **知识库归档（长文本边际爆炸，调研《长文本知识库归档_SOTA》）**：
  - 证据/实体加 `last_seen`+`occurrence`+`status(active/archived/refuted)`+`refuted_by`（collect 时记录，零成本）
  - `is_active_cluster()` 活跃层过滤：recent(≤30章) or frequent(≥3章) or weighted(≥4) or confirmed(≥2)，四条件 OR（长程伏笔保护）
  - `consolidate()` 代际压缩（mine 前执行）：过期低频证据→archived（不参与推理，文件保留），不活跃簇→跳过推理
  - **参数配置化**：`settings.json run.archive`（w_recent/k_freq/k_confirm/every），config.py 读取，clue_agent 函数可显式覆盖，无硬编码
  - 验证：第一章过期低频证据自动归档；51/86 不活跃簇被跳过；17 结论保留
- [ ] **优化 C hyperedges 超边**：clue_graph/setting_graph 加 n 元超边（P2，差异化卖点）
- [ ] **优化 E extract+clue 合并调用**：需 A/B 验证小模型稳定性（暂缓）
- [ ] **BIRCH 式增量聚类**：新证据只与簇质心比，O(n²)→O(簇数)（P2，200章以上才需要）

### P1 · 旧阶段遗留待办
- [ ] 浏览器实操验证 Web Demo UI（拖拽→识别→开始→下载）
- [ ] 删除旧 HTML 生成（book_detail/knowledge_graph/final_report/stage3_viz_report）及代码引用
- [ ] 移除 final_report「本地 vs 云端」对比逻辑
- [ ] 全仓扫描硬编码《诡秘之主》文案
- [ ] attention 切分接入 demo + 与 v2 对比

### P2 · 去本地化、去硬编码
- [x] **产物路径重构**：stage1 过程数据 → `outputs/<书>_<日期>/stage1/`（db 副本）；stage2 → `stage2/`（含 rag_answers）；RAG → `rag/`；最终 `index.html` + `xx_数据.xlsx` 在根
- [x] **Excel 导出**：`src/export_excel.py`（纯 stdlib 手写 xlsx，db 每表一个 sheet）
- [x] 修复 gen_personality 硬编码《诡秘之主》（`周明瑞` 残留 → StopIteration）
- [x] 去除 alias_linked 等硬编码默认参数
- [x] 全链路验证：viz 一键跑通（exit 0），index 渲染 + xlsx 结构验证通过
- [x] **三模型后端**：`llm.config.json` 预设 xiaohongshu(小红书 dots.ai, 默认) / glm(智谱 GLM-4-Flash, 免费) / qwen3(本地 Ollama)；`llm_client` 支持 ENABLE_THINKING 关闭深度思考；key 从环境变量/--key 读取
- [x] **Web Demo 调通**（`_web_demo.py` + `_demo_run.py`）：上传→识别格式/章数→输入章节数→选模型→拆书→**下载产物**；已验证识别(1309章)、启动(POST /start)、进度轮询、下载(index.html) 全链路
- [ ] 用浏览器实际操作验证 UI 交互（拖拽→识别→开始→下载）

### P1 · 收敛产物到单 index.html（核心已落地，收尾中）
- [x] **单页 Tab 版 index.html**：拆人（含**六芒星雷达图**）/ 拆章 / 拆设定关系（SVG 图谱），Tab 切换
- [x] 模板固化：`src/index_template.html` + cli.py `archive_stage3` 注入版
- [x] 移除 final_report「本地 vs 云端」对比入口（Tab 版不再引用）
- [ ] 删除 `book_detail.html` / `knowledge_graph.html` / `final_report.html` / `stage3_viz_report.html` 生成 + 代码引用（export_graph 仍产出 graph_data.json 数据，HTML 可不生成）
- [ ] `detail_data.json` / `graph_data.json` / `personality.json` 保留为数据源（供 index 内嵌）

### P2 · 去本地化、去硬编码
- [ ] 移除 `final_report.py` 的「本地 vs 云端」对比逻辑（未来只用免费 API）
- [ ] 全仓扫描硬编码《诡秘之主》文案（stage2 已修，还有 build_detail / export_graph / 模板等）
- [ ] `llm_client.py` 验证免费 API 兼容（openai 后端已有，配 key 即可）
- [ ] 移除本地模型验证相关代码路径（loadtest / probe 等保留或归档）

### P2.5 · 人物档案信息完整性（已修复核心，可复查）
- [x] **定位根因**：`gen_characters_chunked` 主角全书场景一次性输入超 num_ctx(4096)，Ollama 静默截断只留末尾章节 → 档案只反映最后几章
- [x] **修复**：两阶段生成（分块摘要 ≤8章/块 → 合并终稿），主角 50 章全覆盖
- [x] 实测：齐夏事件 [49,50] → [1,6,10,13,22,30,33,43]；全部 30 人首现章=最早事件章 ✓
- [ ] 复查各角色档案质量（身份/关系/弧光是否有误）

### P2.6 · 读者注意力切分 stage1（已实现，待接入 demo/对比）
- [x] 新增 `extract_mode="attention"`：LLM 动态切分+程序 8-15 段规整，解决数据块过小
- [x] 修复 attention 抽取 actinfo 全空 bug（`_job` 分支漏 attention）
- [x] 实测 10 章：字数 300-500 占比 59%（优于规则 53%），零碎片
- [ ] 接入 `_demo_run.py` / web 流程（用户可切换 attention 模式）
- [ ] vs v2 规则模式做端到端抽取质量对比（actinfo / who 准确度）

### P3 · 门户前端（阶段 D 主体）
- [ ] 站点结构：方法论页 / 知识体系页 / demo 页
- [ ] 上传组件：拖拽 + 进度 + 错误态
- [ ] 对接后端转换 API（demo 服务演进为正式 API）
- [ ] 部署方案：免费 API key 配置 + 静态托管

---

## 三、进展记录

### 2026-08-25（读者注意力切分 · 三模型）
- ✅ **新增 stage1 「读者注意力」动态切分模式**（`extract_mode="attention"`）：
  - `src/extract.py`：`split_attention()`（LLM 模拟读者阅读，动态分场景）+ `_normalize_bounds()`（程序侧强制 8-15 段规整，避免碎片/超大）+ `split_attention_user` prompt
  - `main.py`：attention 模式替代规则切分，逐章动态分场景
  - `cli.py extract --extract-mode attention`：命令行可切换
- ✅ **实测 10 章**（GLM-4-Flash）对比规则切分：
  | 指标 | 规则切分 | 注意力(程序规整) |
  |---|---|---|
  | 每场景段落数 | 均12.6 最小2 最大15 | 均12.8 最小6 最大16 |
  | 每场景字数 | 均358 最小15 最大610 | 均373 最小111 最大601 |
  | **字数300-500占比** | 53% | **59%** |
  | 碎片(<6段) | 有 | **0** |
  - ✅ **解决了"数据块过小"问题**：最小段数 2→6，最小字数 15→111，无碎片
- ⚠️ **发现并修复 Bug：attention 模式 actinfo 全空**
  - **现象**：attention 模式 stage1，场景 actinfo 为空（73/74）
  - **根因**：`extract_all()` 的 `_job` 分支只认 `"v2"`/`"single"`，`extract_mode="attention"` 落入默认分支 → 用了旧 `extract_scene`（5W1H schema），actinfo 字段不匹配被过滤为空
  - **修复**：`extract.py` `_job` 中 `if extract_mode == "v2" or extract_mode == "attention"` 都走 `extract_scene_v2`
  - **验证**：修复后 actinfo 均 5.1 条，0 空，88% ≥3 条
- ✅ **测试并确认 GLM-4-Flash 输出上限**：`max_tokens` 可设 8000+，实测能完整输出 2299-3714 token（3917字）自然结束；之前截断是 `num_predict` 设小（2500）所致，非模型上限
- ✅ **llm_client 关思考双保险**：GLM 用官方 `thinking:{"type":"disabled"}` + dots.ai 用 `chat_template_kwargs.enable_thinking=false`；三模型实测 JSON 正常、无 reasoning
- ✅ **三模型 stage1 单场景耗时实测**：小红书 dots.ai 4.2s（最快）、GLM-4-Flash 16.4s、qwen3:8b 18.4s（本地最慢）
- ✅ **修复 _ollama_chat base 覆盖 bug**：之前强制传 `base=C.OLLAMA_BASE`，覆盖了 env 的云端 `LLM_BASE_URL`，导致云端 backend 请求打到 localhost 404

### 2026-08-23（今天）
- ✅ **epub 原生支持合入 `src/ingest.py`**：`_epub_ingest()` + `ingest()` 按扩展名自动分流；纯 stdlib 零依赖
- ✅ **修复 epub max_chapters 边界**：spine 多读第 51 章 → 统一为 50 章
- ✅ **修复跨小说污染**：`config.py` DB_PATH 按小说名隔离（`stage1_v2_50_十日终焉.db`）；`main.py` 抽取缓存加小说名
- ✅ **修复 stage2 两个 bug**：db 路径改走 `C.DB_PATH`；上下文参数 NUM_CTX 跟随 config（修复 qwen3:8b 下 Ollama 500）；提示词改用 `C.NOVEL_NAME`
- ✅ **修复 build_detail db 硬编码**：加 `--db` 参数（修复 detail_data 误读旧《诡秘之主》库 → 51 章 → 50 章）
- ✅ **《十日终焉》(epub) 前 50 章全链路跑通**：
  - Stage1：306 场景切分 → LLM 抽取 304/306 成功（103 分钟，qwen3:8b）
  - Stage2：50 章纲 / 30 人物 / 9 设定 / 全书总结
- ✅ **单页 Tab 版 index.html（产物收敛核心）**：
  - 三 Tab：**拆人**（人物卡片 + 弹层画像 + **六芒星雷达图**）/ **拆章**（章纲卡片）/ **拆设定关系**（SVG 力导向图谱 + 悬停高亮）
  - 内嵌 detail_data + graph_data + personality，单文件自包含，零外部依赖
  - 去掉 final_report「本地 vs 云端」对比模块
  - 模板固化 `src/index_template.html`，cli.py `archive_stage3` 注入生成
  - 已用 Edge 无头模式验证真实渲染（char-card / 六芒星 SVG / ch-card / Tab 全部就位）+ node 语法校验通过
- ✅ **修复人物档案信息缺失（超上下文截断）**：`gen_characters_chunked` 改为两阶段（分块摘要→合并终稿）；主角 50 章全覆盖；齐夏事件从仅 [49,50] 修复为横跨 [1,6,10,13,22,30,33,43]
  - Stage3：8 产物 + index.html，健康检查 **16/16 全过**
- ✅ **编写 Web Demo 三件套**：`_web_demo.py`（HTTP 服务）/ `_demo_run.py`（后台 runner）/ `web_demo.html`（页面内嵌 JS）
- ⏳ 待联调 demo 服务

### 2026-08-22（源码修复日）
- ✅ 7 个损坏源码全部修复：cli.py / build_detail.py / export_graph.py / final_report.py / compare_stage2.py / check_viz.py / gen_personality.py
- ✅ 11 个核心环节编译 + 导入验证全绿
- ✅ `cli.py viz` 一键链路端到端跑通；健康检查 16/16
- ✅ 目录清理：400 个 "blat" 垃圾文件、59+15 个 `_` 调试脚本、3 个 cdp 浏览器数据目录、`_corrupt_backup`、git dump 文件、`_ji.log`/`_pip.log`
- ✅ 保留 `.venv_exp`（torch/transformers 实验环境 4.7GB）

---

## 四、常用命令

```bash
# 跑全书（settings.json 指定 novel.path / chapters）
python src/cli.py extract --backend local   # stage1
python src/cli.py build  --backend local    # stage2
python src/cli.py viz    --backend local    # stage3 一键可视化

# Web Demo（拖入小说）
python _web_demo.py 8080                    # 打开 http://localhost:8080

# 健康检查
python src/check_viz.py
```
